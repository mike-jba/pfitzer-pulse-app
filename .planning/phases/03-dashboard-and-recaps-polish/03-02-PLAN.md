---
phase: 03-dashboard-and-recaps-polish
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/data/dashboard.ts
  - src/components/dashboard/recent-calls-table.tsx
autonomous: true

must_haves:
  truths:
    - "Recent Calls rows for fully-processed calls show the real agent name and primary category"
    - "Recent Calls rows for calls without completed AI analysis show a 'Pending' badge in the Agent and Category cells"
    - "Unanalyzed calls are still listed — they are not hidden"
  artifacts:
    - path: "src/lib/data/dashboard.ts"
      provides: "processing_status added to Recent Calls query and RecentCall type"
      contains: "processing_status"
    - path: "src/components/dashboard/recent-calls-table.tsx"
      provides: "Pending badge rendering for agent/category when analysis incomplete"
      contains: "Pending"
  key_links:
    - from: "src/components/dashboard/recent-calls-table.tsx"
      to: "RecentCall.processing_status"
      via: "badge condition processing_status !== 'complete'"
      pattern: "processing_status"
---

<objective>
Fix the Agent and Category display in the dashboard Recent Calls list. Calls that
have finished AI analysis show the real agent name and primary category; calls that
have NOT finished analysis show a "Pending" badge in those two cells instead of a
bare em-dash. Unanalyzed calls remain visible. Closes DASH-05.

Purpose: Karen and Garret can tell at a glance which calls are still being processed
versus which have a real (missing) agent/category, rather than seeing an ambiguous "—".
Output: `processing_status` threaded through the Recent Calls query and type, and
Pending badges in the table cells.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-dashboard-and-recaps-polish/03-CONTEXT.md
@.planning/phases/03-dashboard-and-recaps-polish/03-RESEARCH.md

# Recent Calls query + RecentCall type — processing_status must be added here
@src/lib/data/dashboard.ts
# Badge component for the Pending badge
@src/components/ui/badge.tsx
</context>

<coordination_note>
The `src/components/dashboard/recent-calls-table.tsx` file is CREATED by plan 03-01
(Task 2). Both plans run in wave 1 but the executor processes plans sequentially.
This plan EDITS that component to add Pending-badge logic. If 03-01 has not yet
created the file when this plan runs, create the file per 03-01's Task 2 spec first,
then apply this plan's badge changes. The `dashboard.ts` query edit below is the
authoritative source for `processing_status` (03-01 intentionally left it untouched).
</coordination_note>

<tasks>

<task type="auto">
  <name>Task 1: Add processing_status to the Recent Calls query and type</name>
  <files>src/lib/data/dashboard.ts</files>
  <action>
    1. In the `RecentCall` type (around lines 33-46), add a field:
       `processing_status: string | null;`
    2. In `getDashboardData()`, the `recentResult` Supabase query `.select(...)` string
       (around line 100-101), append `, processing_status` to the selected columns so the
       value is returned. The full select must include the existing columns PLUS
       `processing_status`.
    3. No other logic change — `recentCalls` is already cast to `RecentCall[]`.
  </action>
  <verify>`npm run build` succeeds — TypeScript confirms processing_status is now part of RecentCall and the query selects it.</verify>
  <done>RecentCall type has processing_status; the recent-calls query selects processing_status.</done>
</task>

<task type="auto">
  <name>Task 2: Render Pending badges for agent and category in incomplete calls</name>
  <files>src/components/dashboard/recent-calls-table.tsx</files>
  <action>
    In the Recent Calls table component, change the Agent and Category cell rendering so
    that when a call's analysis is not complete, a "Pending" badge shows instead of "—".

    Define a per-row boolean: `const isPending = call.processing_status !== 'complete'`.

    A small helper badge (place near the top of the file):
    ```tsx
    function PendingBadge() {
      return (
        <span className="inline-flex rounded-full bg-amber-100 px-2 py-0.5 text-[11px] font-medium text-amber-700">
          Pending
        </span>
      );
    }
    ```

    Agent cell — replace `{call.agent_name_inferred ?? "—"}` with:
    - if `isPending` → `<PendingBadge />`
    - else → `call.agent_name_inferred ?? "—"` (a complete call with a genuinely null
      agent still shows "—", which is correct — analysis ran but no agent was inferred).

    Category cell — replace the current `primary_category ? <Badge> : "—"` block with:
    - if `isPending` → `<PendingBadge />`
    - else if `call.primary_category` → existing `<Badge variant="secondary">…</Badge>`
    - else → `<span className="text-muted-foreground text-xs">—</span>`

    Do NOT hide any rows — every call in `calls` still renders (CONTEXT.md: no hiding of
    unanalyzed rows). The Sentiment and Flags cells can stay as-is (they already degrade to "—").
  </action>
  <verify>`npm run build` and `npm run lint` pass. `npm run dev` — a call whose processing_status is not 'complete' shows a "Pending" amber badge in both Agent and Category cells; completed calls show real values.</verify>
  <done>Incomplete calls show Pending badges for agent + category; complete calls show real agent/category; no rows hidden.</done>
</task>

</tasks>

<verification>
- `npm run build` and `npm run lint` pass.
- In the running app, calls without completed analysis show Pending badges; completed calls show real data.
- Row count in Recent Calls is unchanged (no filtering by status).
</verification>

<success_criteria>
- DASH-05: Agent and Category fields display correctly for fully-processed calls; Pending badge for in-progress calls; unanalyzed rows remain visible.
</success_criteria>

<output>
After completion, create `.planning/phases/03-dashboard-and-recaps-polish/03-02-SUMMARY.md`
</output>
