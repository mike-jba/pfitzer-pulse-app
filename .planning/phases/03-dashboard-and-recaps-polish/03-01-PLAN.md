---
phase: 03-dashboard-and-recaps-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/data/dashboard.ts
  - src/lib/actions/call-detail.ts
  - src/components/calls/call-detail-content.tsx
  - src/components/dashboard/recent-calls-table.tsx
  - src/app/dashboard/page.tsx
autonomous: false

must_haves:
  truths:
    - "Clicking a Recent Calls row opens a slide-out panel showing that call's full detail"
    - "The slide-out panel shows metadata, AI analysis, and transcript without navigating away from the dashboard"
    - "Recent Calls rows are compact with no wasted vertical space between entries"
    - "Closing the panel returns focus to the dashboard with no full-page reload"
  artifacts:
    - path: "src/lib/actions/call-detail.ts"
      provides: "Server Action wrapping getCallDetail for on-demand panel fetch"
      exports: ["getCallDetailAction"]
    - path: "src/components/calls/call-detail-content.tsx"
      provides: "Shared call detail renderer (metadata, analysis, transcript)"
      min_lines: 120
    - path: "src/components/dashboard/recent-calls-table.tsx"
      provides: "Client Recent Calls table managing selected call + Sheet"
      min_lines: 120
  key_links:
    - from: "src/components/dashboard/recent-calls-table.tsx"
      to: "src/lib/actions/call-detail.ts"
      via: "getCallDetailAction(callId) called on row click"
      pattern: "getCallDetailAction"
    - from: "src/app/dashboard/page.tsx"
      to: "src/components/dashboard/recent-calls-table.tsx"
      via: "renders RecentCallsTable with recentCalls prop"
      pattern: "RecentCallsTable"
---

<objective>
Make the dashboard Recent Calls list clickable — a row click opens a slide-out
panel (Sheet) with that call's full detail (metadata, AI analysis, transcript) —
and tighten the row density so there is no wasted vertical space. Closes DASH-03
(click-to-detail as slide-out panel) and DASH-04 (row density).

Purpose: Karen and Garret can inspect any call from the dashboard without losing
their place by navigating to /calls/[id] and hitting Back.
Output: A client Recent Calls table, a shared CallDetailContent component, and a
Server Action that fetches call detail on demand.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-dashboard-and-recaps-polish/03-CONTEXT.md
@.planning/phases/03-dashboard-and-recaps-polish/03-RESEARCH.md

# Current dashboard server component (Recent Calls table is inline here)
@src/app/dashboard/page.tsx
# Recent Calls query + RecentCall type
@src/lib/data/dashboard.ts
# Call detail data layer — getCallDetail() and CallDetailData type to wrap/reuse
@src/lib/data/call-detail.ts
# Full call detail page — source of the sections to extract into CallDetailContent
@src/app/calls/[id]/page.tsx
# Sheet primitive (base-ui Dialog) — default width too narrow, must override
@src/components/ui/sheet.tsx
# Existing Server Action pattern ('use server' + service client)
@src/lib/actions/audit.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract shared CallDetailContent component and Server Action</name>
  <files>src/components/calls/call-detail-content.tsx, src/lib/actions/call-detail.ts, src/app/calls/[id]/page.tsx</files>
  <action>
    Create a reusable presentational component and a Server Action so call detail
    can render both on the /calls/[id] page and inside the dashboard Sheet.

    1. Create `src/lib/actions/call-detail.ts`:
       - First line: `'use server'`
       - Import `getCallDetail` from `@/lib/data/call-detail` and re-export a thin
         wrapper: `export async function getCallDetailAction(id: string) { return getCallDetail(id) }`
       - Follow the 'use server' pattern in `src/lib/actions/audit.ts`. No Zod needed —
         id is a string passed from our own component. Return type is `CallDetailData | null`.

    2. Create `src/components/calls/call-detail-content.tsx` (NO "use client" — it is a
       pure presentational component, safe to render in client OR server trees):
       - Props: `{ call: CallDetailData; transcriptLines: TranscriptLine[] }` (import both
         types from `@/lib/data/call-detail`).
       - Move these from `src/app/calls/[id]/page.tsx` into this file (copy the JSX and the
         helper sub-components/formatters they need):
         - Formatters: `fmt`, `fmtDateTime`, `fmtOutcome`, the `sentimentColors`,
           `statusColors`, `speakerColors`, `speakerColor` maps/fns
         - Sub-components: `MetaRow`, `SectionLabel`, `FlagChip`
         - JSX sections: the status/sentiment/flag chip header row (the `<div className="flex flex-wrap gap-2">` block WITHOUT the `<AuditCallButton>`), the "Call Details" Card, the "AI Analysis" Card, and the "Transcript" Card.
       - EXCLUDE from the shared component: the page-level `<Link>` back button, the
         `<h1>`/caller header block, the `<AuditCallButton>`, and the entire "Processing
         Events" Card (too technical for the dashboard panel per CONTEXT.md).
       - Wrap the returned sections in a `<div className="space-y-4">`.

    3. Refactor `src/app/calls/[id]/page.tsx` to consume the shared component:
       - Keep the page's data fetch (`getCallDetail`, `getActiveAgents`, `parseTranscript`),
         the back `<Link>`, the `<h1>` caller header, the status/sentiment/flags chips row
         WITH `<AuditCallButton>`, AND the "Processing Events" Card (page keeps showing events).
       - Replace the "Call Details" + "AI Analysis" + "Transcript" Cards with
         `<CallDetailContent call={call} transcriptLines={transcriptLines} />`.
       - To avoid the chip header rendering twice: the shared component renders the chips;
         so REMOVE the chips block from the page header and let CallDetailContent own it,
         OR keep chips in the page and have CallDetailContent NOT render chips. Choose: keep
         chips + AuditCallButton in the PAGE header (it needs the audit button), and have
         CallDetailContent render its own chip row at the top of its space-y-4 (the panel has
         no page header). This means chips appear once on the page (page header) and once in
         the panel (CallDetailContent) — acceptable since they are different surfaces. Confirm
         the page does not visually double-render chips by removing the duplicate chip block
         from CallDetailContent's output WHEN used on the page is NOT possible; simplest: page
         header keeps chips+audit button, CallDetailContent does NOT render chips. Move the
         chip rendering OUT of CallDetailContent. Final rule: CallDetailContent renders ONLY
         Call Details Card + AI Analysis Card + Transcript Card. Chips stay in each caller
         (page header for the page; panel adds its own minimal chip row in Task 3).
  </action>
  <verify>`npm run build` succeeds. `npm run lint` passes. /calls/[id] still renders metadata, AI analysis, transcript, and processing events.</verify>
  <done>CallDetailContent renders Call Details + AI Analysis + Transcript cards only. getCallDetailAction returns CallDetailData. /calls/[id] page renders identically to before (events still present).</done>
</task>

<task type="auto">
  <name>Task 2: Build client RecentCallsTable with slide-out panel and tightened rows</name>
  <files>src/lib/data/dashboard.ts, src/components/dashboard/recent-calls-table.tsx, src/app/dashboard/page.tsx</files>
  <action>
    Replace the inline Recent Calls `<table>` in the dashboard with a client component
    that opens a Sheet on row click, and tighten row density.

    1. In `src/lib/data/dashboard.ts`, add `call_outcome` to the Recent Calls data so the
       panel/table can show a one-line outcome (CONTEXT.md asks for outcome/summary column).
       `call_outcome` is denormalized? It is NOT on `calls` — it lives on `call_analysis`.
       Simplest reliable approach: do NOT add an extra column to the table. Instead keep the
       existing columns and rely on the Sheet for full detail. So make NO query change here
       beyond what Task is needed — leave dashboard.ts query as-is. (call_outcome shown inside
       the panel via CallDetailContent.) Skip editing dashboard.ts if no field is needed.

    2. Create `src/components/dashboard/recent-calls-table.tsx`:
       - First line: `"use client"`
       - Props: `{ calls: RecentCall[] }` (import `RecentCall` from `@/lib/data/dashboard`).
       - State: `const [selectedId, setSelectedId] = useState<string | null>(null)` and
         `const [detail, setDetail] = useState<CallDetailData | null>(null)` and
         `const [loading, setLoading] = useState(false)`.
       - Move the existing `<table>` markup from `dashboard/page.tsx` into this component,
         INCLUDING the header card wrapper `<div className="rounded-xl border bg-card shadow-sm">`
         and the `fmt`/`fmtTime`/`sentimentColors` helpers.
       - TIGHTEN ROW DENSITY (DASH-04): change every row/header cell `py-2` to `py-1.5`, change
         header `px-4`→`px-3` and body `px-4`→`px-3`. Remove the stacked caller layout's extra
         vertical gap: keep name + number but make the number `text-[11px] leading-tight`. The
         goal is visibly more compact rows with no wasted vertical whitespace.
       - Make each `<tr>` clickable: add `onClick={() => handleRowClick(call.id)}`,
         `className` additions `cursor-pointer`, and keep the existing hover styling.
       - `handleRowClick(id)`: set selectedId=id, setLoading(true), call
         `const d = await getCallDetailAction(id)` (import from `@/lib/actions/call-detail`),
         setDetail(d), setLoading(false).
       - Render a `<Sheet open={selectedId !== null} onOpenChange={(open: boolean) => { if (!open) { setSelectedId(null); setDetail(null); } }}>`.
         Inside: `<SheetContent side="right" className="sm:max-w-2xl overflow-y-auto">`
         (override the too-narrow default width — see Pitfall #2 in RESEARCH).
         Include a `<SheetHeader>` with a `<SheetTitle>` (required for base-ui accessibility —
         e.g. the caller name or "Call Detail"). Body: if `loading` show a "Loading…" line;
         else if `detail` render `<CallDetailContent call={detail} transcriptLines={parseTranscript(detail.transcript?.transcript_text ?? null)} />`
         (import `parseTranscript` and `CallDetailData` from `@/lib/data/call-detail`, and
         `CallDetailContent` from `@/components/calls/call-detail-content`); else "No detail."
       - Import Sheet parts from `@/components/ui/sheet`: Sheet, SheetContent, SheetHeader, SheetTitle.

    3. In `src/app/dashboard/page.tsx`:
       - Remove the inline Recent Calls `<table>` block and the now-unused `fmtTime`/`sentimentColors`
         helpers (keep `fmt` if still used elsewhere — it is used by KpiCard avg duration; verify).
       - Import `RecentCallsTable` from `@/components/dashboard/recent-calls-table` and render
         `<RecentCallsTable calls={recentCalls} />` where the table card used to be.
       - Page stays a server component (`getDashboardData()` still runs server-side; data passed as prop).
  </action>
  <verify>`npm run build` succeeds. `npm run lint` passes. `npm run dev` — dashboard Recent Calls rows are visibly compact; clicking a row opens a right-side panel ~2xl wide showing metadata + analysis + transcript; closing returns to dashboard with no reload.</verify>
  <done>Rows are clickable and compact. Sheet opens on click, fetches detail via Server Action, renders CallDetailContent, and closes cleanly via onOpenChange.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Clickable, compact Recent Calls list on the dashboard with a slide-out detail panel (metadata, AI analysis, transcript) that does not navigate away from the dashboard.</what-built>
  <how-to-verify>
    1. Run `npm run dev` and open http://localhost:3000/dashboard
    2. Confirm Recent Calls rows look compact — no large vertical gaps between rows.
    3. Click any row. A panel should slide in from the right (~2xl wide) showing the
       caller, Call Details, AI Analysis, and Transcript for that call.
    4. Click the X (or outside) to close. You should remain on the dashboard — no full
       page reload, scroll position preserved.
    5. Click a different row to confirm the panel updates to the new call.
  </how-to-verify>
  <resume-signal>Type "approved" or describe any layout/behavior issues.</resume-signal>
</task>

</tasks>

<verification>
- `npm run build` and `npm run lint` both pass.
- /calls/[id] page is unchanged in behavior (still shows Processing Events + Audit button).
- Dashboard Recent Calls rows are compact and clickable; panel opens/closes correctly.
</verification>

<success_criteria>
- DASH-03: Recent Calls rows open a slide-out panel with full call detail (no navigation).
- DASH-04: Recent Calls layout is tightened — wasted vertical whitespace removed.
- No service-role key exposure (dashboard/page.tsx stays server component; detail fetched via Server Action).
</success_criteria>

<output>
After completion, create `.planning/phases/03-dashboard-and-recaps-polish/03-01-SUMMARY.md`
</output>
