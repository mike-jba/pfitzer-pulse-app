---
phase: 01-foundation-and-security
plan: 03
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/lib/data/constants.ts
  - src/lib/data/calls.ts
  - src/lib/data/dashboard.ts
autonomous: true

must_haves:
  truths:
    - "Calls shorter than the minimum duration threshold do not appear in the Call Explorer"
    - "Calls shorter than the minimum duration threshold are excluded from all dashboard KPIs, charts, and recent-calls list"
    - "'No Digit' robocall/hangup calls are excluded from all views (either by an explicit release_cause filter or by the documented duration proxy)"
  artifacts:
    - path: "src/lib/data/constants.ts"
      provides: "Shared MIN_CALL_DURATION_SECONDS constant"
      contains: "MIN_CALL_DURATION_SECONDS"
    - path: "src/lib/data/calls.ts"
      provides: "getCalls() with duration (and release_cause if present) exclusion"
      contains: "MIN_CALL_DURATION_SECONDS"
    - path: "src/lib/data/dashboard.ts"
      provides: "All five dashboard sub-queries filtered by duration"
      contains: "MIN_CALL_DURATION_SECONDS"
  key_links:
    - from: "src/lib/data/dashboard.ts"
      to: "src/lib/data/constants.ts"
      via: "import shared threshold"
      pattern: "MIN_CALL_DURATION_SECONDS"
---

<objective>
Globally exclude very short calls (DASH-06) and "No Digit" robocall/hangup calls (DASH-07) from every view.

Purpose: Short calls and immediate hangups are noise that distort KPIs and clutter the call list. They must be filtered consistently everywhere, not per-view. A single shared constant prevents drift between the dashboard and the explorer.
Output: A shared threshold constant and duration filters applied across getCalls() and all five getDashboardData() sub-queries, plus a DASH-07 decision (explicit release_cause filter vs documented duration proxy) grounded in actual DB state.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/intel/summary.md

# Data layer to filter (01-01 may have already touched calls.ts — read current state first)
@src/lib/data/calls.ts
@src/lib/data/dashboard.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Decide DASH-07 approach from live DB state</name>
  <files>(investigation only — no file changes)</files>
  <action>
    Before coding, determine whether a real `release_cause` field exists for "No Digit" calls. Research found no release_cause anywhere in the codebase, but confirm against the live DB.

    Use `mcp__plugin_supabase_supabase__list_tables` and `mcp__plugin_supabase_supabase__execute_sql` on project jdoatvotmsmhrmpitzon:

    1. Does any release-cause-like column exist?
       SELECT column_name FROM information_schema.columns
       WHERE table_name = 'calls'
       AND (column_name ILIKE '%release%' OR column_name ILIKE '%cause%' OR column_name ILIKE '%disposition%' OR column_name ILIKE '%digit%');

    2. Inspect raw/portal metadata that might already carry it:
       SELECT column_name, data_type FROM information_schema.columns WHERE table_name = 'calls';
       (Scan for a jsonb/raw column. If one exists, sample a few rows to see if "No Digit" appears.)

    3. Duration profile of likely robocalls:
       SELECT duration_seconds, COUNT(*) FROM calls WHERE duration_seconds <= 5 GROUP BY duration_seconds ORDER BY duration_seconds;

    Decide:
    - Approach B (explicit filter) ONLY if a usable release_cause value is actually present in the DB today. Then filter where that value = 'No Digit'.
    - Approach A (duration proxy) otherwise: "No Digit" calls are 0-3 seconds and are already removed by the DASH-06 duration filter. Document that DASH-07 is satisfied via the proxy and that adding a true release_cause field is a deferred pipeline change (would require n8n HTML parsing + schema + re-ingest, out of v2 scope).

    If MCP is unavailable, ask the user to confirm whether the portal exposes a release cause. Record the chosen approach with evidence.
  </action>
  <verify>Column-existence query run; a documented decision (Approach A or B) with supporting evidence captured for the SUMMARY.</verify>
  <done>DASH-07 approach is chosen based on real DB state, not assumption.</done>
</task>

<task type="auto">
  <name>Task 2: Add shared threshold constant and filter getCalls()</name>
  <files>src/lib/data/constants.ts, src/lib/data/calls.ts</files>
  <action>
    Create src/lib/data/constants.ts exporting:
      export const MIN_CALL_DURATION_SECONDS = 25;
    Add a comment noting the threshold is the DASH-06 business value and is adjustable. (If Task 1 surfaced a clearly different real-world cutoff, use that and note it; otherwise 25.)

    In src/lib/data/calls.ts (read the CURRENT file first — 01-01 may have modified it):
    - Import MIN_CALL_DURATION_SECONDS from './constants'.
    - Add `.gt('duration_seconds', MIN_CALL_DURATION_SECONDS)` to the getCalls() query so calls at or below the threshold are excluded. (Use .gt so exactly 25s is excluded per ">25s keeps it".)
    - If Task 1 chose Approach B, also add the release_cause exclusion (e.g. `.not('release_cause', 'eq', 'No Digit')` or `.or` as appropriate). If Approach A, no release_cause code is added — the duration filter covers it.

    Note on null durations: `.gt('duration_seconds', N)` excludes NULL rows in Postgres. If Task 1 shows legitimate calls can have NULL duration_seconds, decide whether to keep them; default behavior of excluding nulls is acceptable for noise reduction — note the choice.
  </action>
  <verify>
    `npm run build` passes.
    Grep calls.ts confirms MIN_CALL_DURATION_SECONDS import and a .gt('duration_seconds' filter.
    Optional DB sanity: SELECT COUNT(*) FROM calls WHERE duration_seconds > 25; matches the number of rows the explorer should now show.
  </verify>
  <done>getCalls() excludes short calls (and 'No Digit' calls if Approach B).</done>
</task>

<task type="auto">
  <name>Task 3: Apply the same filter to all dashboard sub-queries</name>
  <files>src/lib/data/dashboard.ts</files>
  <action>
    In src/lib/data/dashboard.ts, import MIN_CALL_DURATION_SECONDS from './constants' and add `.gt('duration_seconds', MIN_CALL_DURATION_SECONDS)` to ALL FIVE sub-queries inside the Promise.all block in getDashboardData():
    1. yesterdayResult (count)
    2. weekResult (count)
    3. monthResult (KPIs + categories source)
    4. volumeResult (call volume chart)
    5. recentResult (recent calls list)

    Every one must get the filter so KPIs, both charts, and the recent-calls list all exclude short calls consistently. If Task 1 chose Approach B, add the same release_cause exclusion to all five as well.

    Do not change the aggregation logic — only add the filter clause to each Supabase query builder chain.
  </action>
  <verify>
    `npm run build` passes.
    Grep dashboard.ts confirms exactly five `.gt('duration_seconds'` occurrences (one per sub-query).
    Loading /dashboard shows KPI counts that exclude short calls.
  </verify>
  <done>All five dashboard sub-queries exclude short calls (and 'No Digit' if Approach B).</done>
</task>

</tasks>

<verification>
- `npm run build` passes.
- MIN_CALL_DURATION_SECONDS defined once and imported in both calls.ts and dashboard.ts.
- Five duration filters present in dashboard.ts; one in calls.ts.
- DASH-07 approach documented with DB evidence.
</verification>

<success_criteria>
Short calls (≤ threshold) are excluded from the Call Explorer and from every dashboard KPI, chart, and the recent-calls list. "No Digit" calls are excluded via an explicit filter or the documented duration proxy.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-and-security/01-03-SUMMARY.md`
recording the chosen threshold value and the DASH-07 approach (A or B) with rationale.
</output>
