---
phase: 01-foundation-and-security
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/api/ingest/analysis/route.ts
  - src/lib/data/calls.ts
autonomous: true

must_haves:
  truths:
    - "Root cause of the Call Explorer blank state is identified from live DB state, not guessed"
    - "Call Explorer (/calls) displays the existing call records"
    - "Calls that finish the pipeline reach processing_status = 'complete'"
  artifacts:
    - path: "src/app/api/ingest/analysis/route.ts"
      provides: "Resilient denorm update that does not silently strand calls at 'transcribed'"
      contains: "validateIngestAuth"
    - path: "src/lib/data/calls.ts"
      provides: "getCalls() returning the rows that exist in the DB"
  key_links:
    - from: "src/app/audits/page.tsx and src/app/calls"
      to: "src/lib/data/calls.ts getCalls()"
      via: "server component data fetch"
      pattern: "getCalls"
---

<objective>
Identify and fix the root cause of the Call Explorer showing no data (DATA-01).

Purpose: This is the milestone's blocking bug. Karen and Garret cannot see any call records, which makes the entire dashboard useless. Research identified a likely silent-failure path in the analysis denorm update that strands calls at processing_status = 'transcribed', but the root cause MUST be confirmed against live DB state before any code is written.
Output: A confirmed diagnosis and a targeted fix that makes existing call records appear in the Call Explorer.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/intel/summary.md

# Files central to this bug
@src/lib/data/calls.ts
@src/app/api/ingest/analysis/route.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Diagnose DATA-01 from live DB state (no code yet)</name>
  <files>(investigation only — no file changes)</files>
  <action>
    Do NOT write any code in this task. First gather ground truth from the live Supabase project (ID: jdoatvotmsmhrmpitzon).

    Use the Supabase MCP tools available to you:
    - `mcp__plugin_supabase_supabase__list_tables` to confirm the `calls` table columns.
    - `mcp__plugin_supabase_supabase__execute_sql` to run these queries:

    1. Status distribution — are calls stranded before 'complete'?
       SELECT processing_status, COUNT(*) FROM calls GROUP BY processing_status;

    2. Does the denorm target column exist?
       SELECT column_name FROM information_schema.columns
       WHERE table_name = 'calls'
       AND column_name IN ('booking_made','primary_category','sentiment','follow_up_required','complaint_flag','sales_opportunity_flag','agent_name_inferred','call_time_ct','call_date','duration_seconds');

    3. Are there ANY rows at all, and do they have category/summary populated?
       SELECT id, processing_status, primary_category, call_time_ct, duration_seconds FROM calls ORDER BY call_time_ct DESC LIMIT 20;

    4. Does call_analysis have rows that never denormed back?
       SELECT COUNT(*) FROM call_analysis;

    If MCP tools are unavailable or error, STOP and ask the user to run these queries in the Supabase dashboard and paste results.

    Record the findings. Determine which hypothesis holds:
    - H1 (denorm column missing → silent update failure → calls stuck at 'transcribed'): true if a column referenced in the analysis denorm update (lines ~97-106) is missing from the calls table AND calls are stuck before 'complete'.
    - H2 (getCalls returns rows but the page renders empty): true if calls exist with data but UI is blank — note getCalls() currently does NOT filter on processing_status, so investigate the page component if rows exist.
    - H3 (zero rows / env issue): true if the calls table is empty.

    Write the confirmed diagnosis into the eventual SUMMARY.
  </action>
  <verify>Query results captured for all 4 queries; one hypothesis explicitly confirmed with evidence.</verify>
  <done>Root cause is stated with supporting DB evidence (not a guess).</done>
</task>

<task type="auto">
  <name>Task 2: Apply targeted fix based on confirmed diagnosis</name>
  <files>src/app/api/ingest/analysis/route.ts, src/lib/data/calls.ts</files>
  <action>
    Apply ONLY the fix that matches the confirmed diagnosis from Task 1.

    If H1 (denorm column missing) confirmed — preferred fix is to make the pipeline resilient so a denorm hiccup never strands a call:
    - In src/app/api/ingest/analysis/route.ts, split the denorm update so that processing_status='complete' is set even if the optional denorm fields fail. Do FIRST a minimal status update:
        await supabase.from('calls').update({ processing_status: 'complete' }).eq('id', call_id)
      then attempt the full denorm of category/sentiment/flags as a best-effort second update whose failure is logged but non-fatal.
    - Only if Task 1 proved a specific column (e.g. booking_made) is genuinely missing AND the user confirms it should exist: add the column via `mcp__plugin_supabase_supabase__apply_migration` with a single ALTER TABLE statement (e.g. ADD COLUMN booking_made boolean DEFAULT false). Prefer the resilience fix over schema changes unless the column is clearly required by other reads.
    - After fixing the code, push stranded calls to complete with one SQL statement via MCP:
        UPDATE calls SET processing_status='complete'
        WHERE processing_status='transcribed'
        AND id IN (SELECT call_id FROM call_analysis);
      Run this only if Task 1 showed call_analysis rows exist for stranded calls.

    If H2 (rows exist, UI blank for another reason) confirmed:
    - The fix is in the read/render path. Read src/app/calls (page + table component) and src/lib/data/calls.ts. getCalls() currently returns ALL rows ordered by call_time_ct. If rows have null call_time_ct they sort oddly or a client filter hides them — fix the specific culprit you identified. Do not add speculative changes.

    If H3 (zero rows) confirmed:
    - This is not a code bug. Document that the pipeline has not populated data and stop; surface this to the user in the SUMMARY as the finding. Do not invent data.

    Keep changes minimal and scoped to the confirmed cause.
  </action>
  <verify>
    Re-run the status distribution query — affected calls now show 'complete'.
    Run `npm run build` — passes with no type errors.
    Re-run: SELECT id, processing_status, primary_category FROM calls ORDER BY call_time_ct DESC LIMIT 20; confirm rows now eligible for display.
  </verify>
  <done>Existing call records appear in the Call Explorer (or, for H3, the absence of data is documented as the finding). Build passes.</done>
</task>

</tasks>

<verification>
- DB query confirms calls reach processing_status = 'complete'.
- `npm run build` passes.
- Visiting /calls in the running app shows call rows (if data exists in DB).
</verification>

<success_criteria>
Call Explorer displays call records, and the root cause of the prior blank state is documented with DB evidence. The analysis ingest route no longer silently strands calls before 'complete'.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-and-security/01-01-SUMMARY.md`
including the confirmed root-cause hypothesis and the exact fix applied.
</output>
