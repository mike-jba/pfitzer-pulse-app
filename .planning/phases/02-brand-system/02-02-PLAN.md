---
id: 02-02
phase: 02-brand-system
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - src/app/layout.tsx
  - src/app/dashboard/page.tsx
  - src/app/calls/[id]/page.tsx
  - src/app/audits/page.tsx
  - src/app/audits/[id]/page.tsx
  - src/app/recaps/page.tsx
  - src/app/trends/page.tsx
  - src/app/settings/page.tsx
  - src/components/calls/call-explorer.tsx
autonomous: false
must_haves:
  truths:
    - "Pages feel dense and professional — no excessive padding around or between sections"
    - "Main content wrapper padding is tightened (p-6 -> p-4)"
    - "Every page root uses consistent vertical rhythm (space-y-4)"
    - "Table rows are tighter (py-3 -> py-2) in dashboard and call explorer"
    - "All tabs share the same visual language: same sidebar, header, card, and table treatment"
  artifacts:
    - path: "src/app/layout.tsx"
      provides: "Tightened main wrapper padding"
      contains: "p-4"
    - path: "src/components/calls/call-explorer.tsx"
      provides: "Tighter table row padding"
  key_links:
    - from: "page roots"
      to: "consistent spacing"
      via: "space-y-4 utility"
      pattern: "space-y-4"
---

# Plan 02-02: Spacing Pass and Visual Consistency

<objective>
Remove the excessive whitespace that makes the app feel padded, and confirm a consistent
visual language across every tab. This is a surgical spacing pass: reduce the main wrapper
padding, normalize page vertical rhythm to space-y-4, and tighten table rows. Then a
human verifies the dense, consistent look across all pages.

Purpose: Phase 2 success requires the app feel like a tight, professional product, not a
padded template. Tokens (02-01) gave it brand color; this gives it brand density.
Output: Tightened spacing across layout + all pages + tables, verified by human.

Depends on 02-01: brand tokens must be applied first so the human verification in Task 4
judges the final look (color + density together).
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Phase research (exact spacing locations)
@.planning/phases/02-brand-system/02-RESEARCH.md

# Prior plan in this phase (brand tokens applied)
@.planning/phases/02-brand-system/02-01-SUMMARY.md

# Files this plan edits
@src/app/layout.tsx
@src/app/dashboard/page.tsx
@src/components/calls/call-explorer.tsx
</context>

## Requirements
- BRAND-02: Excessive spacing is fixed across all pages — tighter, more professional layout
- BRAND-03: Consistent visual language across all tabs (sidebar, header, cards, tables)

<tasks>

<task type="auto">
  <name>Task 1: Tighten the main content wrapper and page vertical rhythm</name>
  <files>src/app/layout.tsx, src/app/dashboard/page.tsx, src/app/audits/page.tsx, src/app/recaps/page.tsx, src/app/trends/page.tsx, src/app/settings/page.tsx</files>
  <action>
Reduce the outer padding and per-page vertical rhythm.

In `src/app/layout.tsx` (line 28), the <main> wrapper:
- Change `className="flex-1 overflow-y-auto bg-muted/30 p-6"` to use `p-4`.

Normalize each page root from `space-y-6` to `space-y-4` (these are the exact lines):
- `src/app/dashboard/page.tsx` line 43: `<div className="space-y-6">` -> `space-y-4`
- `src/app/audits/page.tsx` line 46: `space-y-6` -> `space-y-4`
- `src/app/recaps/page.tsx` line 60: `space-y-6` -> `space-y-4`
- `src/app/trends/page.tsx` line 6: `space-y-6` -> `space-y-4`
- `src/app/settings/page.tsx` line 6: `space-y-6` -> `space-y-4`

Only change the page-root spacing container. Do not alter inner grid/gap classes in this
task.
  </action>
  <verify>
`npm run build` passes. Grep `space-y-6` across src/app/**/page.tsx returns no matches.
Grep layout.tsx confirms `p-4` on <main> (no `p-6`).
  </verify>
  <done>
Main wrapper is p-4; all five listed page roots use space-y-4; no page-root space-y-6 left.
  </done>
</task>

<task type="auto">
  <name>Task 2: Normalize the two detail pages to space-y-4</name>
  <files>src/app/calls/[id]/page.tsx, src/app/audits/[id]/page.tsx</files>
  <action>
The two detail pages use `space-y-5`. Bring them in line with the rest at `space-y-4`:
- `src/app/calls/[id]/page.tsx` line 152: `<div className="space-y-5">` -> `space-y-4`
- `src/app/audits/[id]/page.tsx` line 35: `<div className="space-y-5">` -> `space-y-4`

This ensures every page (list and detail) shares the same vertical rhythm.
  </action>
  <verify>
`npm run build` passes. Grep `space-y-5` across src/app returns no matches.
  </verify>
  <done>
Both detail pages use space-y-4. No space-y-5 remains in src/app.
  </done>
</task>

<task type="auto">
  <name>Task 3: Tighten table row padding in dashboard and call explorer</name>
  <files>src/app/dashboard/page.tsx, src/components/calls/call-explorer.tsx</files>
  <action>
Reduce table cell vertical padding from py-3 to py-2 so tables are denser. Keep horizontal
px-4 unchanged.

In `src/app/dashboard/page.tsx` recent-calls table — change `py-3` to `py-2` on the
<th> header cells (lines ~129-135) and the <td> body cells (lines ~156,159,167,170,175,184,198).
Leave the `border-b px-5 py-4` section header (line 122) as-is — that is a card header, not
a row.

In `src/components/calls/call-explorer.tsx`:
- Line 487 <th>: `px-4 py-3` -> `px-4 py-2`
- Line 516 <td>: `px-4 py-3` -> `px-4 py-2`
- Leave line 470 (`px-5 py-4` toolbar) and line 531 (`px-4 py-3` pagination footer) as-is —
  those are not data rows and tightening them would crowd controls.

Do NOT change the shadcn `Card` component padding (src/components/ui/card.tsx) — research
confirms cards are already consistent; its py-4 is structural.
  </action>
  <verify>
`npm run build` passes. Dashboard and call-explorer data-row cells use py-2. Card component
unchanged.
  </verify>
  <done>
Dashboard recent-calls rows and call-explorer header/body rows use py-2; toolbar, section
headers, pagination, and Card component padding unchanged.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Applied the full Phase 2 brand system: Brand Orange primary + Brand Blue sidebar + colored
charts + Montserrat/Open Sans fonts + branded logo (02-01), then tightened spacing (main p-4,
page roots space-y-4, denser table rows) across every page.
  </what-built>
  <how-to-verify>
1. Run `npm run dev` and open http://localhost:3000.
2. Walk every tab in the sidebar: Dashboard, Call Explorer, Trends, Recaps, Audits, Settings.
3. Confirm visual consistency (BRAND-03):
   - Sidebar looks the same on every page: dark navy blue, active item is lighter blue.
   - Logo box is brand orange with the logo/Shield icon.
   - Cards share the same rounded/border/shadow style everywhere.
   - Tables (Dashboard recent calls, Call Explorer) share the same row style and feel dense.
4. Confirm density (BRAND-02): pages feel tight and professional, not padded. No large empty
   gaps around or between sections.
5. Confirm brand (BRAND-01): dashboard chart bars are colored (orange/blue/green/amber/purple),
   primary buttons are brand orange, body font is Open Sans, headings are Montserrat.
6. Optionally toggle dark mode (if a toggle exists) and confirm brand orange/blue still read well.
  </how-to-verify>
  <resume-signal>Type "approved" or describe any pages that still feel padded or inconsistent.</resume-signal>
</task>

</tasks>

<verification>
- `npm run build` — no errors
- No `space-y-6` or `space-y-5` in src/app page roots
- `<main>` uses p-4
- Dashboard + call-explorer data rows use py-2
- Human confirms dense, consistent, branded look across all six tabs
</verification>

<success_criteria>
- Main wrapper p-4, all page roots space-y-4, data-table rows py-2
- Visual language consistent across all tabs (sidebar, header, cards, tables)
- Human-verified: app feels dense, professional, and on-brand
</success_criteria>

<output>
After completion, create `.planning/phases/02-brand-system/02-02-SUMMARY.md`
</output>
